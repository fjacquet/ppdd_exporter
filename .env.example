# Copy to .env and fill in — `.env` is gitignored, never commit real secrets.
# Used by docker-compose.server.yml.

# Exporter image tag (GHCR). Pin a release for production instead of :latest.
PPDD_TAG=latest

# Hostname (or IP) of the DD appliance. config.yaml can reference it as ${DD01_HOSTNAME}.
# DD01_HOSTNAME=dd01.example.com

# Read-only DD monitoring username. config.yaml can reference it as ${DD01_USERNAME}.
# DD01_USERNAME=ppdd-monitor

# Password for the DD monitoring user. config.yaml references it as ${DD01_PASSWORD}.
DD01_PASSWORD=changeme

# Grafana admin login.
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=changeme

# Public URL Grafana is served at (your server, not localhost) — needed so login
# redirects and share links resolve correctly when accessed remotely.
GF_SERVER_ROOT_URL=http://your-server.example.com:3000
