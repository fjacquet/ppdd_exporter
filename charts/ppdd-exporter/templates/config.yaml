{{- if and (not .Values.existingSecret) .Values.config }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ppdd-exporter.fullname" . }}-config
  labels:
    {{- include "ppdd-exporter.labels" . | nindent 4 }}
stringData:
  config.yaml: {{ tpl .Values.config . | quote }}
{{- end }}
